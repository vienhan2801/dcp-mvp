import AppShell from "@/components/layout/AppShell";
export default function ManufacturerLayout({ children }: { children: React.ReactNode }) {
  return <AppShell type="manufacturer">{children}</AppShell>;
}
